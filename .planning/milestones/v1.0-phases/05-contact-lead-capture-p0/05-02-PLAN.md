---
phase: 5-contact-lead-capture-p0
plan: 05-02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/components/Footer.tsx
  - apps/backend/app/Http/Controllers/Api/ThemeController.php
  - apps/backend/routes/api.php
  - apps/frontend/app/admin/settings/theme/page.tsx
  - apps/frontend/lib/admin-api.ts
  - apps/frontend/components/admin/sidebar.tsx
  - apps/backend/tests/Feature/ThemeTest.php
autonomous: true
requirements: [FR-10, FR-6]
must_haves:
  truths:
    - "Footer newsletter form submits email to POST /api/subscribe"
    - "Newsletter shows inline success message after subscription"
    - "Newsletter shows 'Already subscribed' message for duplicate emails"
    - "Admin can update theme settings (colors, fonts) via PUT /api/admin/theme"
    - "Theme changes apply immediately on save — public site uses updated CSS vars"
    - "Admin theme settings page accessible from sidebar Theme link"
  artifacts:
    - path: "apps/frontend/components/Footer.tsx"
      provides: "Footer with functional newsletter subscribe form"
    - path: "apps/backend/app/Http/Controllers/Api/ThemeController.php"
      provides: "ThemeController with index (GET) and update (PUT) methods"
    - path: "apps/frontend/app/admin/settings/theme/page.tsx"
      provides: "Admin theme settings page with color pickers and font selectors"
    - path: "apps/frontend/lib/admin-api.ts"
      provides: "fetchThemeSettings and updateThemeSettings API functions"
    - path: "apps/frontend/components/admin/sidebar.tsx"
      provides: "Updated sidebar with Theme link pointing to /admin/settings/theme"
  key_links:
    - from: "apps/frontend/components/Footer.tsx"
      to: "apps/backend/app/Http/Controllers/Api/SubscribeController.php"
      via: "POST /api/subscribe fetch call with email body"
      pattern: "fetch.*api/subscribe"
    - from: "apps/frontend/app/admin/settings/theme/page.tsx"
      to: "apps/backend/app/Http/Controllers/Api/ThemeController.php"
      via: "PUT /api/admin/theme fetch call with theme settings body"
      pattern: "fetch.*api/admin/theme"
    - from: "apps/backend/app/Http/Controllers/Api/ThemeController.php"
      to: "apps/backend/app/Models/ThemeSetting.php"
      via: "ThemeSetting::updateOrCreate to persist theme changes"
      pattern: "ThemeSetting::(updateOrCreate|firstOrCreate)"
---

<objective>
Wire the footer newsletter subscribe form to the existing POST /api/subscribe endpoint, build the admin theme settings page with PUT endpoint, and update the sidebar navigation.

Purpose: Newsletter subscription is a key lead capture mechanism (FR-10). Theme settings management (FR-6) enables admin to rebrand the site without code changes. Both features complete the Phase 5 contact/lead capture scope.

Output: Functional newsletter subscribe in footer, admin theme settings page at /admin/settings/theme, and PUT /api/admin/theme endpoint.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SPEC.md
@docs/ERROR-HANDLING.md
@packages/shared/src/schemas/subscriber.ts
@apps/backend/app/Http/Controllers/Api/SubscribeController.php
@apps/backend/app/Http/Controllers/Api/ThemeController.php
@apps/frontend/components/Footer.tsx
@apps/frontend/components/admin/sidebar.tsx
@apps/frontend/lib/admin-api.ts
</context>

<tasks>

<task type="auto">
  <name>Wire footer newsletter subscribe form to POST /api/subscribe</name>
  <files>apps/frontend/components/Footer.tsx</files>
  <read_first>
    - apps/frontend/components/Footer.tsx (current footer — has newsletter UI but no form submission)
    - packages/shared/src/schemas/subscriber.ts (SubscribeRequestSchema — email validation)
    - apps/backend/app/Http/Controllers/Api/SubscribeController.php (existing store method)
    - apps/backend/app/Http/Requests/SubscribeRequest.php (validation: email, unique constraint)
    - docs/SPEC.md lines 455-491 (POST /api/subscribe contract, response shapes)
    - docs/ERROR-HANDLING.md line 139 (duplicate email handling — 422 with "already subscribed")
    - docs/ux-designs/ux-adsvance-cms-2026-07-18/EXPERIENCE.md (newsletter states: success, duplicate, invalid)
  </read_first>
  <action>
Modify `apps/frontend/components/Footer.tsx` to make the newsletter form functional:

1. Convert the newsletter section from static HTML to a controlled form:
   - Add useState for: email (string), submitting (boolean), success (boolean), duplicate (boolean), error (string | null)
   - The email input becomes a controlled component bound to email state
   - The Subscribe button becomes type="submit" on a form element

2. Add form submission handler:
   - On submit, prevent default
   - Client-side validation: check email is non-empty and matches basic email regex
   - POST to `${process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api'}/subscribe` with JSON body `{ email }`
   - On 201 response: set success=true, clear email field
   - On 422 response: parse errors object — if errors.email includes "already subscribed" message, set duplicate=true; otherwise set error from message
   - On 429 response: set error="Too many attempts. Try again in a minute."
   - On network error: set error="Could not subscribe. Please try again."

3. Add inline feedback states below the form:
   - Success: green text "Subscribed!" (per EXPERIENCE.md)
   - Duplicate: yellow/orange text "Already subscribed." (per EXPERIENCE.md)
   - Error: red text with error message
   - These states auto-dismiss after 5 seconds (setTimeout clear)

4. Disable the submit button and input during submission, show "..." on button

5. Use CSS custom properties for styling (no hardcoded colors):
   - Button background: var(--color-primary)
   - Success text: var(--color-success)
   - Error text: var(--color-error)
   - Input background: var(--color-muted) (already used)

6. Keep the existing layout and styling intact — only add form functionality
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/components/Footer.tsx has a form element wrapping the email input and subscribe button
    - Form onSubmit handler prevents default and calls POST /api/subscribe
    - On 201: "Subscribed!" message displayed, email input cleared
    - On 422 with unique violation: "Already subscribed." message displayed
    - On 429: rate limit error message displayed
    - Submit button disabled during submission
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Footer newsletter form submits to POST /api/subscribe with inline success/duplicate/error feedback per UX spec.</done>
</task>

<task type="auto">
  <name>Admin theme settings page with PUT endpoint and sidebar link</name>
  <files>apps/backend/app/Http/Controllers/Api/ThemeController.php, apps/backend/routes/api.php, apps/frontend/app/admin/settings/theme/page.tsx, apps/frontend/lib/admin-api.ts, apps/frontend/components/admin/sidebar.tsx, apps/backend/tests/Feature/ThemeTest.php</files>
  <read_first>
    - apps/backend/app/Http/Controllers/Api/ThemeController.php (current: index only, no update method)
    - apps/backend/app/Models/ThemeSetting.php (fillable: primary_color, secondary_color, accent_color, background_color, foreground_color, muted_color, muted_foreground_color, border_color, success_color, error_color, body_font, heading_font)
    - apps/backend/routes/api.php (current routes — no PUT /api/admin/theme)
    - apps/frontend/lib/admin-api.ts (no theme functions — need fetchThemeSettings, updateThemeSettings)
    - apps/frontend/components/admin/sidebar.tsx (Theme link points to '#')
    - apps/frontend/app/admin/services/page.tsx (pattern for admin CRUD pages — form, validation, toast)
    - docs/SPEC.md lines 109-126 (theme_settings table schema), lines 378-406 (GET /api/theme response)
    - docs/SPEC.md line 947-970 (Theme System — CSS custom properties, ThemeProvider)
    - docs/ux-designs/ux-adsvance-cms-2026-07-18/DESIGN.md (theme settings UI specs)
    - apps/frontend/components/ThemeProvider.tsx (how theme CSS vars are applied — FALLBACK_THEME)
  </read_first>
  <action>
**Step 1: Add ThemeController@update (PUT endpoint)**

Modify `apps/backend/app/Http/Controllers/Api/ThemeController.php`:
- Add `update(Request $request)` method inside `auth:sanctum` middleware group
- Validate incoming fields against the same column list as ThemeSetting fillable
- All fields nullable (admin can save partial theme — only changed fields)
- Use `ThemeSetting::updateOrCreate([], $validated)` to ensure single-row pattern
- Return `$this->success($theme->only([...columns]))` with updated values
- Wrap in try/catch, return 500 on failure

**Step 2: Register PUT route**

Modify `apps/backend/routes/api.php`:
- Inside the `auth:sanctum` group, add: `Route::put('/admin/theme', [ThemeController::class, 'update']);`
- This goes AFTER the existing `Route::get('/theme', ...)` public route (line 42)

**Step 3: Add admin-api.ts theme functions**

Add to `apps/frontend/lib/admin-api.ts`:
- `ThemeSettingsData` interface with all 12 theme fields (all optional strings)
- `fetchThemeSettings(): Promise<{ data: ThemeSettingsData }>` — GET /admin/theme (use request() helper)
- `updateThemeSettings(data: Partial<ThemeSettingsData>): Promise<{ data: ThemeSettingsData }>` — PUT /admin/theme

**Step 4: Create admin theme settings page**

Create `apps/frontend/app/admin/settings/theme/page.tsx`:
- Client component following admin-services page pattern
- Load existing theme on mount via fetchThemeSettings()
- Display form with:
  - Color pickers (HTML input type="color") for: primary, secondary, accent, background, foreground, muted, muted-foreground, border, success, error
  - Font selectors (text inputs or dropdowns) for: body_font, heading_font
  - Live preview panel showing sample text with current colors applied
- Save handler: updateThemeSettings(formData), show toast on success
- Use existing shadcn/ui Card, Button, Label components
- Loading skeleton while fetching

**Step 5: Update sidebar Theme link**

Modify `apps/frontend/components/admin/sidebar.tsx`:
- Change Theme item href from '#' to '/admin/settings/theme'
- This removes the dead link and connects to the new page

**Step 6: Add feature test**

Create or append to `apps/backend/tests/Feature/ThemeTest.php`:
- `test_get_theme_returns_empty_when_no_settings` — GET /api/theme returns { data: {} }
- `test_get_theme_returns_settings` — Create ThemeSetting, GET /api/theme returns values
- `test_admin_can_update_theme_settings` — PUT /api/admin/theme with auth, assert values saved
- `test_update_theme_creates_settings_if_none_exist` — PUT without existing record creates one
- `test_unauthenticated_cannot_update_theme` — PUT without token returns 401
  </action>
  <verify>
    <automated>cd apps/backend && php artisan test --filter=ThemeTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ThemeController has update() method that accepts PUT with auth:sanctum
    - PUT /api/admin/theme route registered in auth:sanctum group
    - admin-api.ts has ThemeSettingsData interface, fetchThemeSettings(), updateThemeSettings()
    - apps/frontend/app/admin/settings/theme/page.tsx exists with color pickers and font selectors
    - Sidebar Theme link points to /admin/settings/theme
    - php artisan test --filter=ThemeTest exits 0 (all tests pass)
    - GET /api/theme returns current theme settings
    - PUT /api/admin/theme persists settings and returns updated values
  </acceptance_criteria>
  <done>Admin can manage theme settings (12 color/font fields) via /admin/settings/theme page. PUT /api/admin/theme persists changes. Footer newsletter subscribes via POST /api/subscribe. Sidebar Theme link navigates to settings page. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Newsletter subscribe POST crosses from untrusted browser to Laravel API |
| admin→API | Theme settings PUT crosses from authenticated admin to Laravel API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-05 | Tampering | POST /api/subscribe | medium | mitigate | Server-side validation via SubscribeRequest, unique email constraint prevents duplicates |
| T-5-06 | Denial of Service | POST /api/subscribe | medium | mitigate | Rate limit 3/min per IP via RateLimiter in AppServiceProvider |
| T-5-07 | Elevation of Role | PUT /api/admin/theme | high | mitigate | auth:sanctum middleware required — unauthenticated requests return 401 |
| T-5-08 | Injection | Theme settings fields | low | mitigate | All fields are nullable strings, no HTML execution — stored as plain text in DB |
| T-5-09 | Info Leak | Theme GET endpoint | low | accept | Public GET /api/theme intentionally exposes color/font settings for frontend theming |
</threat_model>

<verification>
1. `cd apps/frontend && npx tsc --noEmit` — TypeScript compiles cleanly
2. `cd apps/frontend && npm run build` — SSG build succeeds
3. `cd apps/backend && php artisan test --filter=ThemeTest` — all tests pass
4. Manual: visit footer, enter email, click Subscribe — see "Subscribed!" message
5. Manual: visit /admin/settings/theme — see color pickers, change primary color, save — see toast
6. Manual: visit public site — CSS vars reflect updated theme colors
</verification>

<success_criteria>
- Footer newsletter form submits email to POST /api/subscribe
- Success/duplicate/error messages display inline per UX spec
- PUT /api/admin/theme persists theme settings with auth protection
- Admin theme settings page renders with color pickers and font selectors
- Sidebar Theme link navigates to /admin/settings/theme
- Theme changes apply via ThemeProvider CSS vars on next page load
- All backend tests pass
</success_criteria>

<output>
Create `.planning/phases/5-contact-lead-capture-p0/05-02-SUMMARY.md` when done
</output>
