---
phase: 04-frontend-public-pages-p0
plan: 04-02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/frontend/app/(public)/page.tsx
  - apps/frontend/components/ServicesGrid.tsx
  - apps/frontend/components/TeamGrid.tsx
  - apps/frontend/components/PricingTable.tsx
  - apps/frontend/components/LatestPosts.tsx
autonomous: true
requirements: [FR-7, FR-8]

must_haves:
  truths:
    - Public homepage renders with all sections (PageRenderer, Services, Team, Pricing, LatestPosts, Contact CTA)
    - Blog listing page renders with skeleton loaders for loading state (D-07)
    - Blog single post page renders with SEO metadata
    - 404 page renders with theme colors
    - Skeleton loaders are used for data-dependent sections (D-07)
    - All public pages use CSS custom properties for colors
  artifacts:
    - apps/frontend/app/(public)/page.tsx (modified - Suspense boundaries)
    - apps/frontend/components/ServicesGrid.tsx (verified)
    - apps/frontend/components/TeamGrid.tsx (verified)
    - apps/frontend/components/PricingTable.tsx (verified)
    - apps/frontend/components/LatestPosts.tsx (verified)
    - apps/frontend/app/(public)/not-found.tsx (verified)
    - apps/frontend/app/(public)/blog/page.tsx (verified)
    - apps/frontend/app/(public)/blog/[slug]/page.tsx (verified)
  key_links:
    - Homepage to all section components render correctly
    - Blog listing to skeleton loaders during client-side fetch
    - Blog single post to SSG with generateStaticParams
    - 404 to theme-colored error page
---

<objective>
Add Suspense boundaries with skeleton loader fallbacks to homepage sections, verify all public pages render correctly with theme CSS variables, and confirm the full public site works end-to-end.

Purpose: Completes the public site polish by adding loading states for data-dependent sections (D-07) and verifying all pages render. This is the expansion that builds out from the tracer's proven theme + pricing + contact wiring.

Output: Homepage with Suspense-wrapped sections, verified public pages, final build confirmation.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4-frontend-public-pages-p0/4-CONTEXT.md
@.planning/phases/4-frontend-public-pages-p0/04-01-SUMMARY.md
@apps/frontend/app/(public)/page.tsx
@apps/frontend/components/ServicesGrid.tsx
@apps/frontend/components/TeamGrid.tsx
@apps/frontend/components/PricingTable.tsx
@apps/frontend/components/LatestPosts.tsx
@apps/frontend/components/ui/skeleton.tsx
@apps/frontend/app/(public)/not-found.tsx
@apps/frontend/app/(public)/blog/page.tsx
@apps/frontend/app/(public)/blog/[slug]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suspense boundaries with skeleton fallbacks for homepage sections</name>
  <files>apps/frontend/app/(public)/page.tsx</files>
  <behavior>
    - ServicesGrid section shows skeleton card placeholders while loading
    - TeamGrid section shows skeleton card placeholders while loading
    - PricingTable section shows skeleton pricing card placeholders while loading
    - LatestPosts section shows skeleton blog card placeholders while loading
    - PageRenderer section shows skeleton hero placeholder while loading
    - Each skeleton fallback matches the visual shape of its corresponding loaded component
    - Sections that return null (empty data) hide the skeleton gracefully
  </behavior>
  <action>
Per D-07: Add React Suspense boundaries to the homepage (app/(public)/page.tsx) wrapping each data-dependent section with skeleton loader fallbacks.

1. Import Suspense from 'react' and Skeleton from '@/components/ui/skeleton'

2. Create skeleton fallback components for each section:
   - ServicesSkeleton: 4 skeleton cards in a grid (matching ServicesGrid 4-col desktop layout). Each card: rounded-xl border, skeleton icon circle (h-16 w-16), skeleton title (h-5 w-1/2), skeleton description (h-4 w-full).
   - TeamSkeleton: 4 skeleton cards in a grid (matching TeamGrid layout). Each card: rounded-xl border, skeleton avatar circle (h-16 w-16), skeleton name (h-5 w-1/2), skeleton role (h-4 w-1/3).
   - PricingSkeleton: 3 skeleton pricing cards in a grid (matching PricingTable 3-col layout). Each card: rounded-xl border, skeleton title (h-6 w-1/3), skeleton price (h-8 w-1/4), 3 skeleton feature lines (h-4).
   - LatestPostsSkeleton: 3 skeleton blog cards in a grid (matching LatestPosts 3-col layout). Each card: skeleton image (aspect-video), skeleton title (h-5 w-3/4), skeleton excerpt (h-4 w-full).
   - HeroSkeleton: Full-width skeleton hero with heading (h-12 w-1/2) and subtext (h-6 w-2/3).

3. Wrap each section import in Suspense with matching fallback:
   - Suspense fallback={HeroSkeleton} around PageRenderer
   - Suspense fallback={ServicesSkeleton} around ServicesGrid
   - Suspense fallback={TeamSkeleton} around TeamGrid
   - Suspense fallback={PricingSkeleton} around PricingTable
   - Suspense fallback={LatestPostsSkeleton} around LatestPosts

4. Keep the About section and Contact CTA section as-is (they do not fetch data).

5. The skeleton cards should use var(--color-border) for borders and var(--color-muted) for background to match the theme. Use the existing Skeleton component from components/ui/skeleton.tsx.

Do NOT use fenced code blocks in action descriptions. Reference identifiers and file paths only.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
Homepage wraps PageRenderer, ServicesGrid, TeamGrid, PricingTable, and LatestPosts in Suspense boundaries with matching skeleton fallbacks. Skeleton loaders use theme CSS variables. TypeScript compiles cleanly. Lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full public site verification - all pages render with theme CSS vars</name>
  <files>apps/frontend/app/(public)/not-found.tsx, apps/frontend/app/(public)/blog/page.tsx, apps/frontend/app/(public)/blog/[slug]/page.tsx</files>
  <action>
Per D-01, D-05, D-07: Verify all public pages render correctly with theme CSS custom properties.

1. 404 page (not-found.tsx) - Verify it uses var(--color-primary) for the 404 heading and CTA button, var(--color-foreground) for heading, var(--color-muted-foreground) for description. Already implemented - confirm no hardcoded colors.

2. Blog listing (blog/page.tsx) - Verify it uses skeleton loaders (already implemented per Phase 03). Confirm CSS vars used for colors. Confirm client-side pagination works with var(--color-border) and var(--color-foreground).

3. Blog single post (blog/[slug]/page.tsx) - Verify it uses var(--color-background) for background, var(--color-foreground) for text, var(--color-primary) for back link. Confirm SSG with generateStaticParams works.

4. Run full SSG build: cd apps/frontend && npm run build. Confirm all pages generate: /, /blog, /blog/[slug], 404. Confirm no build errors.

5. Run TypeScript check: cd apps/frontend && npx tsc --noEmit. Confirm no type errors.

6. Run lint: cd apps/frontend && npm run lint. Confirm no lint errors.

Do NOT use fenced code blocks in action descriptions.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit && npm run lint && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
All public pages render correctly: homepage with all sections, blog listing with skeleton loaders, blog single post with SEO metadata, 404 with theme colors. Full SSG build succeeds. TypeScript and lint pass. No hardcoded brand colors anywhere in public pages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build time to Static HTML | All data fetched at build time; no client-side API calls for public pages |
| Skeleton to Loaded state | Visual transition from skeleton to real content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Denial of Service | SSG build with API down | medium | mitigate | ThemeProvider fallback (Plan 04-01) ensures build succeeds |
| T-04-05 | Information Disclosure | Skeleton content | low | accept | Skeletons show layout shape only, no data |
</threat_model>

<verification>
1. cd apps/frontend && npx tsc --noEmit - TypeScript compiles without errors
2. cd apps/frontend && npm run lint - Lint passes
3. cd apps/frontend && npm run build - SSG build completes successfully
4. Homepage sections wrapped in Suspense with skeleton fallbacks
5. All public pages use CSS custom properties (no hardcoded brand colors)
6. Blog listing has skeleton loaders for loading state
7. Blog single post renders with SEO metadata
8. 404 page renders with theme colors
</verification>

<success_criteria>
- Public homepage renders all sections with Suspense + skeleton fallbacks (D-07)
- Blog listing renders with skeleton loaders (D-07)
- Blog single post renders with SSG and SEO metadata
- 404 page renders with theme CSS variables
- All public pages use CSS custom properties for colors (D-01)
- Full SSG build succeeds
- TypeScript and lint pass
</success_criteria>

<output>
Create .planning/phases/4-frontend-public-pages-p0/04-02-SUMMARY.md when done
</output>
