---
phase: 04-frontend-public-pages-p0
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/components/ThemeProvider.tsx
  - apps/frontend/components/PricingTable.tsx
  - apps/frontend/app/(public)/page.tsx
autonomous: true
requirements: [FR-7, FR-8]

must_haves:
  truths:
    - Theme CSS custom properties are injected at build time from API settings (D-01)
    - Theme falls back to hardcoded defaults when API is unavailable (D-02)
    - Admin panel is independent of public theme (D-03)
    - Pricing table CTA links to /contact page (D-06)
    - Homepage contact section is a CTA link to /contact (D-06)
    - Popular plan ribbon uses accent color (D-04)
  artifacts:
    - apps/frontend/components/ThemeProvider.tsx (modified)
    - apps/frontend/components/PricingTable.tsx (modified)
    - apps/frontend/app/(public)/page.tsx (modified)
  key_links:
    - ThemeProvider to fetchTheme() to CSS vars on :root
    - PricingTable CTA to /contact page
    - Homepage contact section to /contact page
---

<objective>
Fix ThemeProvider to fall back gracefully when API is unavailable, update PricingTable CTA to link to /contact page, and replace homepage placeholder contact section with a CTA link to /contact.

Purpose: Ensures theme integration works with fallback colors (D-02), pricing table CTA directs users correctly (D-06), and homepage contact section is functional (D-06). This is the tracer that proves theme + pricing + contact wiring works end-to-end.

Output: ThemeProvider with graceful fallback, PricingTable with /contact CTA, homepage with contact CTA link.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4-frontend-public-pages-p0/4-CONTEXT.md
@apps/frontend/components/ThemeProvider.tsx
@apps/frontend/components/PricingTable.tsx
@apps/frontend/app/(public)/page.tsx
@apps/frontend/lib/api.ts
@apps/frontend/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThemeProvider graceful fallback + PricingTable CTA + Contact section CTA</name>
  <files>apps/frontend/components/ThemeProvider.tsx, apps/frontend/components/PricingTable.tsx, apps/frontend/app/(public)/page.tsx</files>
  <behavior>
    - ThemeProvider: When fetchTheme() returns null (API unavailable), buildCssVars() uses hardcoded fallback colors from globals.css :root instead of throwing
    - ThemeProvider: When fetchTheme() returns theme data, CSS vars are injected with API values (per D-01)
    - ThemeProvider: Throws only when both fetchTheme() fails AND no fallback can be constructed
    - PricingTable: CTA button links to /contact instead of #contact (per D-06)
    - PricingTable: Popular plan ribbon displays with accent color (per D-04)
    - Homepage: Contact section renders as CTA link to /contact page instead of placeholder text (per D-06)
    - Homepage: All existing sections (PageRenderer, ServicesGrid, TeamGrid, PricingTable, LatestPosts) continue to render
  </behavior>
  <action>
Per D-01, D-02, D-04, D-06:

1. ThemeProvider.tsx - Modify the catch block in ThemeProvider to NOT throw when fetchTheme() returns null or fails. Instead, construct cssVars using hardcoded fallback colors matching the defaults in globals.css :root block:
   - primary_color fallback: '#FF0000'
   - secondary_color fallback: '#fb3d03'
   - accent_color fallback: '#FFC107'
   - background_color fallback: '#FFFFFF'
   - foreground_color fallback: '#333333'
   - muted_color fallback: '#f5f5f5'
   - muted_foreground_color fallback: '#888888'
   - border_color fallback: '#f0f0f0'
   - success_color fallback: '#22c55e'
   - error_color fallback: '#ef4444'
   - body_font fallback: 'Poppins'
   - heading_font fallback: 'Poppins'
   
   The buildCssVars function already accepts a ThemeData object. Create a FALLBACK_THEME constant with these values and use it when fetchTheme() returns null. Remove the NFR-8 error throw - the site should build gracefully with fallback colors (per D-02).

2. PricingTable.tsx - Change the CTA a href="#contact" to a href="/contact" (per D-06). The popular ribbon already works with accent color - verify it uses var(--color-accent) or var(--color-primary) correctly.

3. Homepage (page.tsx) - Replace the placeholder contact section (lines 35-47) with a CTA link to /contact. The section should have:
   - Heading: "Get in Touch"
   - Subtext: "Ready to grow your business? Let's talk."
   - CTA button linking to /contact with primary color background
   - Per D-06, this keeps Phase 4 scope tight - Phase 5 builds the full form

Do NOT use fenced code blocks in action descriptions. Reference identifiers and file paths only.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
ThemeProvider falls back to hardcoded colors when API is unavailable (build succeeds without API). PricingTable CTA links to /contact. Homepage contact section is a functional CTA link to /contact. TypeScript compiles cleanly. Lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify theme CSS vars are applied and pricing table renders responsively</name>
  <files>apps/frontend/components/ThemeProvider.tsx, apps/frontend/components/PricingTable.tsx</files>
  <action>
Per D-01, D-04, D-05:

1. ThemeProvider verification - Confirm the inline style tag injects :root { --color-primary: ...; ... } with the correct CSS custom property names matching globals.css @theme block. The sanitizeCssValue and sanitizeFont functions must handle the fallback values correctly.

2. PricingTable responsive verification - Confirm the grid uses grid-cols-1 md:grid-cols-2 lg:grid-cols-3 (per D-05 single column mobile, 2-3 columns desktop). Confirm popular plan ribbon is positioned with absolute -top-3 left-1/2 -translate-x-1/2 and uses accent/primary color background. Confirm features list renders with check/xmark icons using success/error colors.

3. Run the SSG build to verify all pages generate without errors: cd apps/frontend && npm run build. The build should succeed with or without the Laravel API running (graceful fallback).

Do NOT use fenced code blocks in action descriptions.
  </action>
  <verify>
    <automated>cd apps/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
SSG build completes successfully. Theme CSS vars are correctly structured. Pricing table responsive grid confirmed. Popular ribbon renders with accent color. All public pages (home, blog, blog/[slug], 404) generate static HTML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API to ThemeProvider | Theme data fetched at build time; sanitized before CSS injection |
| API to PricingTable | Pricing data fetched at build time; rendered as static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | ThemeProvider CSS injection | medium | mitigate | sanitizeCssValue and sanitizeFont validate input before CSS injection |
| T-04-02 | Information Disclosure | Theme fallback colors | low | accept | Fallback colors are hardcoded public defaults, no sensitive data |
| T-04-03 | Denial of Service | API unavailable during build | medium | mitigate | Graceful fallback to hardcoded colors per D-02 |
</threat_model>

<verification>
1. cd apps/frontend && npx tsc --noEmit - TypeScript compiles without errors
2. cd apps/frontend && npm run lint - Lint passes
3. cd apps/frontend && npm run build - SSG build completes successfully
4. ThemeProvider uses fallback colors when API is unavailable (no throw)
5. PricingTable CTA links to /contact
6. Homepage contact section links to /contact
</verification>

<success_criteria>
- Theme CSS custom properties applied from API settings with graceful fallback (D-01, D-02)
- Pricing table renders responsively with features, CTA, popular ribbon (D-04, D-05)
- Pricing CTA links to /contact page (D-06)
- Homepage contact section is CTA link to /contact (D-06)
- Admin panel independent of public theme (D-03)
- SSG build succeeds without API (fallback colors)
</success_criteria>

<output>
Create .planning/phases/4-frontend-public-pages-p0/04-01-SUMMARY.md when done
</output>
