---
wave: 3
depends_on: [02-01-PLAN.md, 02-02-PLAN.md]
files_modified:
  - apps/frontend/app/admin/team/page.tsx
autonomous: true
requirements:
  - FR-4 (Manage Team Members — CRUD with photo, bio, sort order)
---

# Plan 02-03: Expansion — Social Links Form Fields + Polish Audit

**Goal:** Add social_links form fields (LinkedIn, Twitter) to the team member form with URL validation, and apply a lightweight consistency audit across all four pages.

**Review findings incorporated:**
- MEDIUM-4 (scope validation): Removed skeleton loading and confirmation dialog re-implementation tasks — these already exist on all 4 pages. Replaced with lightweight consistency audit only.
- LOW (social links URL validation): URL input types + URL validation pattern.

---

## Tasks

### Task 1: Add social_links fields to team member form with URL validation

<read_first>
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/lib/admin-api.ts (TeamMemberData interface — verify social_links shape)
</read_first>

<action>
1. In the form modal, after the Bio field and before Sort Order, add a "Social Links" section with two input fields:
   - **LinkedIn URL**: `<Input type="url">` with label "LinkedIn URL", placeholder "https://linkedin.com/in/username", value bound to `editing.social_links?.linkedin || ''`. The onChange extracts current social_links, updates linkedin field, and sets `{ ...editing, social_links: { ...(editing.social_links || { linkedin: null, twitter: null }), linkedin: value || null } }`.
   - **Twitter URL**: Same pattern with `<Input type="url">`, placeholder "https://twitter.com/username".
2. Group the two fields inside a `<div className="space-y-2">` with a `<Label>Social Links</Label>` header.
3. Initialize `social_links: { linkedin: null, twitter: null }` when opening a new member (in the New Member onClick).
4. On edit (existing member), social_links is already populated from API response — no init needed.
5. Add field-level validation error display for social_links (consistent with 02-02 validation pattern).
</action>

<acceptance_criteria>
- New member form shows LinkedIn and Twitter URL inputs (type="url") under "Social Links" heading
- Editing existing member pre-fills LinkedIn and Twitter values if they exist
- Saving with valid URLs persists social_links to API → appears in subsequent GET
- Saving with empty social links sets them to null on the model
- Invalid URL format triggers browser validation (type="url" provides native validation)
- 422 errors on social_links show inline validation error below the field
- `npx tsc --noEmit` passes, `npm run build` succeeds
</acceptance_criteria>

### Task 2: Lightweight consistency audit — skeleton loading

<read_first>
- apps/frontend/app/admin/services/page.tsx
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/app/admin/pages/page.tsx
- apps/frontend/app/admin/pricing-plans/page.tsx
</read_first>

<action>
1. Audit all four pages to verify skeleton loading is present and column counts match:
   - Services: 5 skeleton cells (Title, Icon, Featured, Sort, Actions) ← verify
   - Team: 5 skeleton cells (Photo+Name, Role, Sort, Actions) ← verify after photo column added in 02-01
   - Pages: 5 skeleton cells (Title, Slug, Status, Sort, Actions) ← verify after Sort column added in 02-02
   - Pricing Plans: 7 skeleton cells (Name, Price, Interval, Popular, Status, Sort, Actions) ← verify
2. If any column count mismatch exists, fix the number of skeleton cells.
3. Do NOT re-implement skeleton loading — it already exists on all pages.
</action>

<acceptance_criteria>
- Skeleton row column count matches actual table columns on all pages
- No broken layout during loading state
- `npx tsc --noEmit` passes, `npm run build` succeeds
</acceptance_criteria>

### Task 3: Lightweight consistency audit — destructive confirmation dialogs

<read_first>
- apps/frontend/app/admin/services/page.tsx
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/app/admin/pages/page.tsx
- apps/frontend/app/admin/pricing-plans/page.tsx
</read_first>

<action>
1. Audit all four pages' AlertDialog confirmations to verify they follow Phase 01.1 D-16 pattern:
   - Title includes resource name (e.g., "Delete service?")
   - Cancel button uses `AlertDialogCancel`
   - Delete button uses `AlertDialogAction` with `variant="destructive"`
2. If any page uses a generic message like "Delete this service? This cannot be undone." instead of including the resource name (e.g., `\`Delete "${deleteTarget?.title}"?\``), update the dialog text to include the resource name for better UX.
3. Do NOT re-implement AlertDialog — it already exists on all pages. This is a text-only audit/polish pass.
</action>

<acceptance_criteria>
- All four delete dialogs include the resource name in the dialog text
- All four use consistent Cancel/Delete button pattern
- `npx tsc --noEmit` passes, `npm run build` succeeds
</acceptance_criteria>

## Verification Criteria

1. `cd apps/frontend && npx tsc --noEmit` passes
2. `cd apps/frontend && npm run build` succeeds
3. Team form has LinkedIn and Twitter URL inputs that save correctly
4. Invalid URLs trigger browser-level validation on type="url" inputs
5. All four pages have correct skeleton column counts
6. All four delete dialogs include the resource name (not generic text)

## Artifacts This Phase Produces

**Modified frontend files:**
- `apps/frontend/app/admin/team/page.tsx` — social_links form fields (LinkedIn, Twitter with type="url" validation)

**Polish improvements (audit-only, no re-implementation):**
- Skeleton column counts verified/corrected across all 4 pages
- Delete confirmation dialog text updated to include resource name across all 4 pages
