---
phase: 06-admin-panel-p0
plan: 06-02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/frontend/app/admin/services/page.tsx
  - apps/frontend/app/admin/team/page.tsx
  - apps/frontend/app/admin/blog-posts/page.tsx
  - apps/frontend/app/admin/pricing-plans/page.tsx
  - apps/frontend/app/admin/pages/page.tsx
autonomous: true
requirements: [FR-1, FR-2, FR-3, FR-4, FR-5]
---

<objective>
Audit and polish all 5 admin CRUD pages for consistent UX: page headers (Title + description + action button), table column structure (Name/Title, Description/Excerpt, Sort Order, Actions), validation patterns, and skeleton loading states.

Purpose: D-04 requires consistent table columns. D-05 requires consistent validation (Zod client-side + Laravel server-side, inline errors). D-07 requires consistent page headers. This plan ensures all 5 CRUD pages follow the same UX patterns.
Output: All 5 admin CRUD pages have consistent headers, table structure, validation, and loading states. TypeScript compiles cleanly.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/6-admin-panel-p0/6-CONTEXT.md
@apps/frontend/app/admin/services/page.tsx
@apps/frontend/app/admin/team/page.tsx
@apps/frontend/app/admin/blog-posts/page.tsx
@apps/frontend/app/admin/pricing-plans/page.tsx
@apps/frontend/app/admin/pages/page.tsx
@apps/frontend/components/admin/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 5 admin CRUD pages for consistent page headers, table columns, and validation</name>
  <files>apps/frontend/app/admin/services/page.tsx, apps/frontend/app/admin/team/page.tsx, apps/frontend/app/admin/blog-posts/page.tsx, apps/frontend/app/admin/pricing-plans/page.tsx, apps/frontend/app/admin/pages/page.tsx</files>
  <read_first>
    - apps/frontend/app/admin/services/page.tsx (full file — reference pattern for consistent headers/tables)
    - apps/frontend/app/admin/team/page.tsx (full file — check header, table columns, validation)
    - apps/frontend/app/admin/blog-posts/page.tsx (full file — check header, table columns, validation)
    - apps/frontend/app/admin/pricing-plans/page.tsx (full file — check header, table columns, validation)
    - apps/frontend/app/admin/pages/page.tsx (full file — check header, table columns, validation)
    - docs/SPEC.md (API shapes and validation rules for each content type)
  </read_first>
  <action>
    Perform a consistency audit across all 5 admin CRUD pages. For each page, verify and fix:

    **Page Headers (per D-07):** Each page should have a consistent header pattern: `<h1>` title + description paragraph + "Add New" button. Verify all 5 pages follow this pattern. If any page is missing a description or uses a different heading style, normalize it.

    **Table Columns (per D-04):** Each CRUD table should have these columns where applicable:
    - Name/Title column (the primary identifier)
    - Description/Excerpt column (truncated if long)
    - Sort Order column (with up/down reorder arrows where applicable)
    - Actions column (Edit + Delete buttons)
    Note: Blog posts may have additional columns (Published status, Reading Time). Pages may have different fields. The core 4 columns must be present.

    **Validation (per D-05):** Verify each page uses:
    - Inline validation errors from 422 response (the `validationErrors` state pattern with `e.errors[field][0]`)
    - `setValidationErrors({})` cleared on successful save or when switching to create mode
    - Error display near the relevant form field

    **Delete Dialogs:** Verify all pages include the resource name in the delete confirmation dialog (pattern from 02-03: `Delete "{resourceName}"?`).

    **Skeleton Loading:** Verify each page has a loading skeleton that matches its table column count.

    Apply fixes ONLY where inconsistencies are found. Do not refactor pages that already match the pattern. The goal is convergence, not rewrites.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit && npm run lint 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - All 5 admin CRUD pages have <h1> title + description + "Add New" button in header
    - All 5 pages have Name/Title, Description/Excerpt, Actions columns in their tables
    - All 5 pages show inline validation errors from 422 responses
    - All 5 pages clear validationErrors on successful save
    - All 5 delete dialogs include the resource name in confirmation text
    - npx tsc --noEmit exits with no errors
  </acceptance_criteria>
  <done>All 5 admin CRUD pages have consistent page headers, table column structure, validation patterns, and delete confirmation dialogs. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→UI | Admin CRUD pages render server data and submit forms |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | Admin CRUD pages | low | accept | All mutations go through auth:sanctum protected API endpoints with server-side validation |
| T-06-04 | Info Disclosure | Admin CRUD pages | low | accept | Pages are behind auth, no sensitive data exposure beyond content titles/descriptions |
</threat_model>

<verification>
- All 5 pages have consistent header pattern (title + description + add button)
- All 5 pages have Name/Title, Description, Actions table columns
- All 5 pages display inline validation errors from 422 responses
- All 5 delete dialogs include resource name
- TypeScript compiles cleanly
</verification>

<success_criteria>
- All admin CRUD pages follow the same UX pattern
- Page headers are consistent across all 5 pages
- Table structure converges to the standard column layout
- Validation error display is consistent
- Delete confirmation dialogs are consistent
</success_criteria>

<output>
Create `.planning/phases/06-admin-panel-p0/06-02-SUMMARY.md` when done
</output>
