---
phase: 06-admin-panel-p0
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/Http/Controllers/Api/Admin/StatsController.php
  - apps/frontend/lib/admin-api.ts
  - apps/frontend/components/admin/stats-overview.tsx
  - apps/frontend/app/admin/dashboard/page.tsx
autonomous: true
requirements: [FR-13]
---

<objective>
Expand dashboard stats to show all 7 content type counts (Services, Team Members, Blog Posts, Pricing Plans, Pages, Messages, Subscribers) with responsive grid and clickable cards.

Purpose: D-01 requires all 7 content type counts visible on the dashboard. D-02 requires responsive grid. D-03 requires cards link to CRUD pages. Currently only 4 counts are shown.
Output: Backend returns 7 counts, frontend renders 7 stat cards in responsive grid, each card links to its CRUD page.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/6-admin-panel-p0/6-CONTEXT.md
@apps/backend/app/Http/Controllers/Api/Admin/StatsController.php
@apps/frontend/lib/admin-api.ts
@apps/frontend/components/admin/stats-overview.tsx
@apps/frontend/app/admin/dashboard/page.tsx
@apps/frontend/components/admin/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand backend StatsController to return 7 content type counts</name>
  <files>apps/backend/app/Http/Controllers/Api/Admin/StatsController.php</files>
  <read_first>
    - apps/backend/app/Http/Controllers/Api/Admin/StatsController.php (current 4-count implementation)
    - apps/backend/app/Models/TeamMember.php (verify model exists and table name)
    - apps/backend/app/Models/PricingPlan.php (verify model exists and table name)
    - apps/backend/app/Models/Page.php (verify model exists and table name)
  </read_first>
  <action>
    Add imports for TeamMember, PricingPlan, and Page models at the top of StatsController.php (per D-01). Add three new safeCount calls in the index() method: `team_members` (TeamMember::class), `pricing_plans` (PricingPlan::class), `pages` (Page::class). The response JSON must include all 7 keys: services, team_members, blog_posts, pricing_plans, pages, unread_messages, subscribers. Use the existing safeCount() helper which already handles missing tables gracefully (returns 0). Per D-01, the dashboard shows all 7 content type counts for complete visibility.
  </action>
  <verify>
    <automated>cd apps/backend && php artisan test --filter=StatsTest 2>/dev/null || echo "NO_STATS_TEST"</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/app/Http/Controllers/Api/Admin/StatsController.php index() returns JSON with keys: services, team_members, blog_posts, pricing_plans, pages, unread_messages, subscribers
    - Each key maps to an integer count
    - GET /api/admin/stats returns 7 numeric fields (verified by curl or test)
  </acceptance_criteria>
  <done>StatsController returns 7 content type counts. GET /api/admin/stats response includes team_members, pricing_plans, pages alongside existing services, blog_posts, unread_messages, subscribers.</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend DashboardStats type, StatsOverview component, and dashboard page</name>
  <files>apps/frontend/lib/admin-api.ts, apps/frontend/components/admin/stats-overview.tsx, apps/frontend/app/admin/dashboard/page.tsx</files>
  <read_first>
    - apps/frontend/lib/admin-api.ts (DashboardStats interface at line 360, fetchAdminStats at line 367)
    - apps/frontend/components/admin/stats-overview.tsx (current 4-card StatsOverview, StatsData interface)
    - apps/frontend/app/admin/dashboard/page.tsx (loading skeleton count, grid layout)
    - apps/frontend/components/admin/sidebar.tsx (verify sidebar links for target pages)
  </read_first>
  <action>
    1. In admin-api.ts: Update DashboardStats interface (line 360) to add `team_members: number`, `pricing_plans: number`, `pages: number` fields alongside existing 4 (per D-01).

    2. In stats-overview.tsx: Update StatsData interface to match DashboardStats (add 3 new fields). Add 3 new StatCard entries to the cards array in StatsOverview:
       - Team Members: title "Team Members", icon Users (already imported), colorClass 'bg-purple-500', href '/admin/team'
       - Pricing Plans: title "Pricing Plans", icon DollarSign (import from lucide-react), colorClass 'bg-cyan-500', href '/admin/pricing-plans'
       - Pages: title "Pages", icon File (import from lucide-react), colorClass 'bg-pink-500', href '/admin/pages'

    3. In dashboard/page.tsx: Update loading skeleton to render 7 placeholder cards instead of 4 (currently `[1,2,3,4].map`). Update the grid to use `lg:grid-cols-4` for responsive layout per D-02 (4 columns desktop, 2 tablet, 1 mobile — the existing `sm:grid-cols-2 lg:grid-cols-4` already handles this).

    Per D-03, stat cards link to their respective admin CRUD pages — the href values handle this.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/lib/admin-api.ts DashboardStats interface contains 7 fields: services, team_members, blog_posts, pricing_plans, pages, unread_messages, subscribers
    - apps/frontend/components/admin/stats-overview.tsx StatsOverview renders 7 StatCard components
    - apps/frontend/components/admin/stats-overview.tsx imports DollarSign and File from lucide-react
    - apps/frontend/app/admin/dashboard/page.tsx loading skeleton renders 7 placeholder cards
    - Grid layout uses sm:grid-cols-2 lg:grid-cols-4 classes
    - npx tsc --noEmit exits with no errors
  </acceptance_criteria>
  <done>Dashboard renders 7 stat cards in responsive 4/2/1 grid. Each card shows count and links to its CRUD page. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Dashboard stats fetched via authenticated GET /api/admin/stats |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | StatsController | low | accept | Read-only query, no user input manipulation possible |
| T-06-02 | Info Disclosure | StatsController | low | accept | Counts only, no sensitive data. Protected by auth:sanctum |
</threat_model>

<verification>
- GET /api/admin/stats returns JSON with 7 numeric keys
- Dashboard page renders 7 stat cards
- Each card links to correct CRUD page
- Responsive grid: 4 cols desktop, 2 tablet, 1 mobile
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Backend StatsController returns 7 content type counts
- Frontend DashboardStats type has 7 fields
- StatsOverview renders 7 clickable stat cards
- Dashboard loading skeleton shows 7 placeholders
- All stat cards link to their respective admin CRUD pages
</success_criteria>

<output>
Create `.planning/phases/06-admin-panel-p0/06-01-SUMMARY.md` when done
</output>
