---
wave: 3
depends_on: [02-01-PLAN.md, 02-02-PLAN.md]
files_modified:
  - apps/frontend/app/admin/team/page.tsx
autonomous: true
requirements:
  - FR-4 (Manage Team Members — CRUD with photo, bio, sort order)
---

# Plan 02-03: Expansion — Social Links Form Fields + Remaining Polish

**Goal:** Add social_links form fields (LinkedIn, Twitter) to the team member form, and apply remaining polish across all four pages to ensure consistency with Phase 01.1 design patterns.

## Tasks

### Task 1: Add social_links fields to team member form

<read_first>
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/app/admin/blog-posts/page.tsx
- apps/frontend/lib/admin-api.ts (TeamMemberData interface — verify social_links shape)
</read_first>

<action>
1. In the form modal, after the Bio field and before Sort Order, add a "Social Links" section with two input fields:
   - **LinkedIn URL**: `<Input>` with label "LinkedIn URL", placeholder "https://linkedin.com/in/username", bound to `editing.social_links?.linkedin || ''`. The onChange extracts current social_links, updates linkedin field, and sets `{ ...editing, social_links: { ...(editing.social_links || { linkedin: null, twitter: null }), linkedin: value || null } }`.
   - **Twitter URL**: Same pattern for `twitter` field, placeholder "https://twitter.com/username".
2. Group the two fields inside a `<div className="space-y-2">` with a `<Label>Social Links</Label>` header.
3. Initialize social_links when opening a new member (in the `New Member` onClick): add `social_links: { linkedin: null, twitter: null }` to the editing state.
4. On edit (existing member), social_links is already populated from the API response if it exists — no init needed.
</action>

<acceptance_criteria>
- New member form shows LinkedIn and Twitter URL inputs under "Social Links" heading
- Editing existing member pre-fills LinkedIn and Twitter values if they exist
- Saving with valid URLs persists social_links to API → appears in subsequent GET
- Saving with empty social links sets them to null on the model
- Form renders with proper spacing and labels
- `npx tsc --noEmit` passes, `npm run build` succeeds
</acceptance_criteria>

### Task 2: Consistent loading-state skeleton across all pages

<read_first>
- apps/frontend/app/admin/services/page.tsx
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/app/admin/pages/page.tsx
- apps/frontend/app/admin/pricing-plans/page.tsx
</read_first>

<action>
1. Audit all four pages to ensure the skeleton loading pattern is consistent:
   - All pages use `Skeleton` component from `@/components/ui/skeleton`
   - All skeleton rows use `Array.from({ length: 4 })` pattern
   - The number of skeleton cells matches the number of table columns including the Sort column (services: 5, team: 5 after adding photo column? — actually keep at Name/Role/Sort/Actions = 4, pages: 4, pricing-plans: 7)
   - Verify all skeleton cells use `<Skeleton className="h-4 w-full" />`
2. For any page where skeleton columns don't match actual column count, fix them.
</action>

<acceptance_criteria>
- All four pages show skeleton loading rows while data is loading
- Skeleton column count matches actual table column count
- No layout shift between skeleton and loaded state
- `npm run build` succeeds
</acceptance_criteria>

### Task 3: Consistent destructive confirmation dialogs

<read_first>
- apps/frontend/app/admin/services/page.tsx
- apps/frontend/app/admin/team/page.tsx
- apps/frontend/app/admin/pages/page.tsx
- apps/frontend/app/admin/pricing-plans/page.tsx
</read_first>

<action>
1. Audit all four pages' AlertDialog confirmations to ensure they follow Phase 01.1 D-16 pattern:
   - Title includes the resource name (e.g., "Delete service?")
   - Description includes: "Are you sure you want to delete \"{name}\"? This action cannot be undone."
   - Cancel button uses `AlertDialogCancel`
   - Delete button uses `AlertDialogAction` with `variant="destructive"`
2. Standardize any deviations:
   - Services page: change description to include the service title, e.g., `Are you sure you want to delete "${deleteTarget?.title}"? This action cannot be undone.`
   - Team page: same — include member name
   - Pages page: same — include page title
   - Pricing plans: same — include plan name
3. After delete, add success toast (if not already present from 02-02).
</action>

<acceptance_criteria>
- All four delete dialogs include the resource name in the confirmation message
- All four delete dialogs have consistent Cancel/Delete button pattern
- Delete button uses `variant="destructive"` on all dialogs
- Deleting shows success toast and removes item from list
- `npm run build` succeeds
</acceptance_criteria>

### Task 4: Fix pages page to add Sort column

<read_first>
- apps/frontend/app/admin/pages/page.tsx
</read_first>

<action>
1. The pages table currently has columns: Title, Slug, Status, Actions. Add a Sort column between Status and Actions.
2. In `load()`, parse sort_order from API response. The Page model has `sort_order` in the schema per SPEC.md.
3. If the API doesn't return `sort_order` for pages, add an issue to verify the model/controller/resource expose it. If it's missing from `PageResource`, add it.
4. Add sort order up/down controls (consistent with 02-02 pattern).
</action>

<acceptance_criteria>
- Pages table shows Sort column with order number
- Up/down controls function and persist
- `npx tsc --noEmit` passes
- `npm run build` succeeds
</acceptance_criteria>

## Verification Criteria

1. `cd apps/frontend && npx tsc --noEmit` passes
2. `cd apps/frontend && npm run build` succeeds
3. Team form has LinkedIn and Twitter URL inputs that save correctly
4. All four pages have consistent skeleton loading pattern
5. All four pages have consistent delete confirmation dialog with resource name
6. Pages table has Sort column with working up/down controls

## Artifacts This Phase Produces

**Modified frontend files:**
- `apps/frontend/app/admin/team/page.tsx` — social_links form fields (LinkedIn, Twitter inputs)

**Pattern consistency improvements across all four pages:**
- Uniform skeleton loading (column count matches table)
- Uniform destructive confirmation dialogs (resource name in message, consistent button pattern)
- Pages table: new Sort column with up/down controls
